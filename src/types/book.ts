

export interface PokemonData {
  abilities: any[]; // Define the type for abilities based on the actual data structure
  base_experience: number;
  forms: any[]; // Define the type for forms based on the actual data structure
  game_indices: any[]; // Define the type for game_indices based on the actual data structure
  height: number;
  held_items: any[]; // Define the type for held_items based on the actual data structure
  id: number;
  is_default: boolean;
  location_area_encounters: string;
  moves: any[]; // Define the type for moves based on the actual data structure
  name: string;
  order: number;
  past_types: any[]; // Define the type for past_types based on the actual data structure
  species: any; // Define the type for species based on the actual data structure
  sprites: any; // Define the type for sprites based on the actual data structure
  stats: any[]; // Define the type for stats based on the actual data structure
  types: any[]; // Define the type for types based on the actual data structure
  weight: number;
  url: string
}

